development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV['REDISCLOUD_URL'] %>
  channel_prefix: live-chat-test_production

 # <%= ENV.fetch("REDIS_URL") { "redis://https://sensei-video-test.herokuapp.com" } %>
